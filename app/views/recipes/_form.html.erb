<%= form_for recipe do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.fields_for :ingredients do |b| %>
    <%= b.collection.check_boxes :recipe_ids, Recipe.all, :id, :name %>
  <% end %>

  <%= f.submit %>
<% end %>
