<%= form_for recipe do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.fields_for :ingredients do |b| %>
    <%= b.collection_check_boxes :ingredients_ids, Ingredient.all, :id, :name %>
  <% end %>

  <%= f.submit %>
<% end %>
